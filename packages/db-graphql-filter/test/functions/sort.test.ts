describe('(Functions) Sort', () => {
  it('should handle undefined sort');
  it('should handle empty sort');

  it('should return only the columns specified by the input query')
  it('should sort asc');
  it('should sort desc');
  it('should sort using the correct subquery when there are multiple subqueries');
  it('should handle sorting with a distinct query');
});
